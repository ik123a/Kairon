services:
  kairon:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kairon-service
    ports:
      - "8080:8080"
    environment:
      - HOST=0.0.0.0
      - PORT=8080
    restart: unless-stopped
